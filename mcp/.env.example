# Database (same as api/.env)
DATABASE_URL=postgresql://user:password@localhost:5432/tritou_notes

# Redis (same as api/.env)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=

# HTTP mode (production) — laisser vide pour le mode stdio (local)
MCP_HTTP_PORT=3001
MCP_AUTH_TOKEN=change_me_with_a_random_secret
